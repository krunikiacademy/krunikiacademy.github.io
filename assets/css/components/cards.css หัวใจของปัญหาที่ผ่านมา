/* Glass card instead of solid black */
.card, .cardLink, .heroCard, .heroBox, .services-hero{
  background: var(--card-glass);
  border: 1px solid var(--card-border);
  border-radius: 22px;
  box-shadow: 0 18px 50px rgba(0,0,0,.55);
}

/* Lock readable text */
.card, .cardLink, .heroCard, .heroBox, .services-hero{
  color: var(--text-main);
}

.card p, .cardLink p{
  color: var(--text-soft);
}
